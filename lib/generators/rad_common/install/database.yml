development:
  adapter: postgresql
  encoding: unicode
  database: rad_common_development

test:
  adapter: postgresql
  encoding: unicode
  database: rad_common_test<%= ENV['TEST_ENV_NUMBER'] %>

production:
  url:  <%= ENV["DATABASE_URL"] %>
  pool: <%= ENV["DB_POOL"] || 5 %>
