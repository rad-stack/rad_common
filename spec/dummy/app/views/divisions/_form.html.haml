= simple_form_for(@division) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name, tooltip: 'bro!'
    = f.input :code

  .form-actions
    = f.button :submit, 'Save', class: 'btn btn-primary'
