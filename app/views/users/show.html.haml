.card.card-default
  = render 'layouts/card_header', no_new_button: true, additional_actions: user_actions(@user)
  .card-body
    = render 'layouts/show_data_columns', data: user_show_data(@user), resource: @user
= render 'users/security'
