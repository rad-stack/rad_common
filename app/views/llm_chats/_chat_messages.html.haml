- logs = llm_chat_logs(llm_chat)
- if logs.present?
  - logs.each do |log|
    - log_data = llm_chat_log_data(llm_chat, log)
    = render log_data[:template], log_data
- else
  = render 'llm_chats/initial_chat_message', llm_chat: llm_chat
