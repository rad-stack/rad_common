= turbo_frame_tag "chat-response-#{params[:response_id]}" do
  - if @bot_response
    - chat_msg = @assistant_session.chat_message_from_log(@bot_response, current_user)
    = render chat_msg.template, **chat_msg.locals
    = turbo_stream.action :scroll_bottom, 'scroll-container'
