.card
  = render 'layouts/card_header',
           action_name: 'custom',
           title: "Notification Settings for #{current_user}",
           icon: 'fa-gear'

  = render 'notification_settings/index', settings: NotificationSetting.settings_for_user(current_user), show_user: false
