= f.input filter.like_input, label: filter.input_label, input_html: { value: search.search_params[filter.like_input] },
                             required: false,
                             tooltip: "Search for keywords or phrases within the #{filter.input_label}"
